# 実装しなければいけないもの一覧

実装しなければいけないものがちゃんと把握できていないと困るのでここに書いておく。
## 見た目関連
### UI関係

- タイトル画面UI
- 設定(config)画面UI
- ステージ選択画面UI
- ステージ画面UI(現在のスコア、階層表示、タイムリミット表示)
- 獲得スコア画面UI
- リザルト画面UI

画面遷移ボタンはPrefabとしてAssets/UI/SceneChangeButtonに作ってあるからそれをベースにアレンジしてつかってください。

### エフェクト

- デバフごとにエフェクトをつけることで何のデバフがかかっているかわかりやすくする必要がある。
- ジャンプした後の着地時のエフェクトも欲しいな

### プレイヤーの見た目
ここら辺はEvenOddさんに投げている

### ステージギミックの見た目形成
READMEにあるステージギミックを、内容のスクリプトとは別に見た目作る

## ステージ関連
### ステージギミック
READMEにあるステージギミックを作成する。ダメージを与えるものはプレイヤーのダメージ判定などを実装し終わった後がいいかも。動く床は実装済み。

### ステージの作成
ステージギミックができてきたら、それをうまく配置してステージを作成する。デバフも考えていい感じのステージが作れるかがキモ。最後の方に難易度調整しながらやればよし。

## プレイヤー関連
- 死亡判定と残基システムの実装
- 死亡判定を呼び出すとリスポーン位置にリスポーンされる機能の実装
- 死亡判定を呼び出すと残基が1減少し、残基が0になるとキャラクターの操作入力を受け取らなくなり、GameOverUIが呼び出されることの実装。
- デバフシステム実装(DebuffManegerでデバフ全般を管理し、これを通してデバフを付与されるように設計)
- それぞれのデバフ増加、デバフを重くするシステムの実装(二段ジャンプ禁止、移動速度低下、ジャンプ高さ低下、遠距離攻撃間隔(クールタイム)増加、被ダメージ、壁衝突の当たり判定を大きくするなどのプレイヤーの操作機能をいじるタイプのデバフの実装。)

## 敵実装

- READMEに書いてある種類の敵を実装する。
- 敵当たり判定
- 敵の攻撃のプレイヤーへの当たり判定
- 敵のスポナーの実装

## スコア実装
- UI関連で書いたことと重複するが、ステージ攻略中にスコアを保持し、UI表示する機能実装。
- ジェム(仮称)の実装、ジェムに接触したらそのオブジェクトを消去し、代わりにジェムスコアを増加させる機能実装。
- クリア時の残り時間を元にタイムスコアを計算する機能実装
- クリア時にスコアデータを階層数をIDとして保存する場所の設計
- クリア時にその階層をクリアしたことと、スコアデータを書き込み、階層数を読み込みIDとして利用するためにリザルト画面に引き継ぐ(Scene遷移しても破棄されない)設計
- 獲得スコア画面とリザルト画面でスコアデータを読み取り、表示する設計←(なぜ直接ステージからスコアデータをもらわないのかというと、ステージクリア時に予期せずリザルトに飛ばずに終了したときに、クリア記録がリザルトに残るようにするため。ステージクリア時に書き込みしておけば安心。)

